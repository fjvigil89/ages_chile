<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="access_client" model="website.menu">
            <field name="name">ACCESO CLIENTES</field> <!-- Name of the menu-->
            <field name="url">/access-client</field>
            <field name="parent_id" ref="website.main_menu"/>
            <field name="sequence" type="int">10</field>
        </record>
        <template id="access_client_page_template">
            <t t-call="website.layout">
                <div id="wrap">
                    <div class="container">
                        <form class="oe_login_form" role="form"
                              t-attf-action="/web/login/access"
                              method="post" onsubmit="this.action = this.action + location.hash">
                            <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>

                            <div class="form-group field-db">

                                <select class="form-control" name="db_selection">
                                    <t t-foreach="selection_options" t-as="option">
                                        <option t-attf-value="#{option}">
                                            <t t-esc="option"/>
                                        </option>
                                    </t>
                                </select>
                            </div>

                            <div class="form-group field-login">
                                <label for="login">Email</label>
                                <input type="text" placeholder="Email" name="login" t-att-value="login" id="login"
                                       t-attf-class="form-control #{'form-control-sm' if form_small else ''}"
                                       required="required"
                                       autofocus="autofocus" autocapitalize="off"/>
                            </div>

                            <div class="form-group field-password">
                                <label for="password">Password</label>
                                <input type="password" placeholder="Password" name="password" id="password"
                                       t-attf-class="form-control #{'form-control-sm' if form_small else ''}"
                                       required="required"
                                       autocomplete="current-password" t-att-autofocus="'autofocus' if login else None"
                                       maxlength="4096"/>
                            </div>

                            <p class="alert alert-danger" t-if="error" role="alert">
                                <t t-esc="error"/>
                            </p>
                            <p class="alert alert-success" t-if="message" role="status">
                                <t t-esc="message"/>
                            </p>

                            <div t-attf-class="clearfix oe_login_buttons text-center mb-1 {{'pt-2' if form_small else 'pt-3'}}">
                                <button type="submit" class="btn btn-primary btn-block">Log in</button>
                                <t t-if="debug">
                                    <button type="submit" name="redirect" value="/web/become"
                                            class="btn btn-link btn-sm btn-block">
                                        Log in as superuser
                                    </button>
                                </t>
                                <div class="o_login_auth"/>
                            </div>

                            <input type="hidden" name="redirect" t-att-value="redirect"/>
                        </form>
                    </div>
                </div>
            </t>
        </template>
        <record id="access_client" model="website.page">
            <field name="name">Acceso clientes</field>
            <field name="website_published">True</field>
            <field name="url">/access-client</field>
            <field name="view_id" ref="access_client_page_template"/>
        </record>


    </data>
</odoo>